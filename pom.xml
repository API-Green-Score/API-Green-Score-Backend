<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>fr.pagesjaunes.pom</groupId>
		<artifactId>pom-ciweb</artifactId>
		<version>6.0-20240124222558</version>
	</parent>

	<groupId>fr.pagesjaunes.api</groupId>
	<artifactId>api-greenscore</artifactId>
	<version>0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
    
	<name>${project.artifactId}</name>

	<properties>

		<!-- ============================================================== -->
		<!-- PROPRIETES DE L'APPLICATION -->
		<!-- ============================================================== -->

		<application.name>api-greenscore</application.name>
		<application.version>${project.version}</application.version>
		<application.finalname>api-greenscore-0</application.finalname>

		<!-- ============================================================== -->
		<!-- PROPRIETES DE L'API -->
		<!-- ============================================================== -->

		<api.name>api-greenscore</api.name>
		<api.version>0</api.version>

	</properties>

	<build>

		<finalName>${application.finalname}</finalName>

		<plugins>
			<!-- Evite le deploiement des artifacts sur Nexus : inutile pour les webapps -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<modules>
		<module>api-greenscore-app</module>
	</modules>
</project>
